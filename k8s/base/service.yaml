apiVersion: v1
kind: Service
metadata:
  name: saju-interpret
  labels:
    app: saju-interpret
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: saju-interpret
